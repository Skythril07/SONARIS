"""Detection verification signals: shadow consistency and physical plausibility (Phase 2).

These reduce false positives *after* detection and georeferencing (plan gates P2.2, P2.3). Each is
a self-contained, ablatable signal that writes onto the Detection contract's Phase-2 fields.
"""
